{
  "name": "Threat Hunting Lab (Python + Jupyter)",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.terminal.activateEnvironment": true,
        "jupyter.interactiveWindowMode": "perFile",
        "jupyter.askForKernelRestart": false
      }
    }
  },

  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt && python -m ipykernel install --user --name threat-hunting-lab --display-name \"Python (Threat Hunting Lab)\"",

  "remoteUser": "vscode"
}
